sudo: false
language: python
python:
        - "2.7"
cache: 
        directories: 
                - $HOME/gcloud/
  env: 
        - GCLOUD=$HOME/gcloud/google-cloud-sdk/bin CLOUDSDK_PYTHON_SITEPACKAGES=1 GOOGLE_APPLICATION_CREDENTIALS=$TRAVIS_BUILD_DIR/client_secret.json.json 

script:
        - python test.py